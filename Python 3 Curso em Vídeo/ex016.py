from math import trunc
x = float(input('Write a value: '))
print(trunc(x))

from random import uniform
#uniform (Gera números reais); randint (Gera números inteiros)
y = uniform(1,10)
print(y)
print(trunc(y))